parameters:
    git_dir: .
    bin_dir: ./vendor/bin
    tasks:
        phpunit: ~
        phpcs:
            standard: PSR2
            ignore_patterns:
                - tests/*.php
        composer:
            no_check_lock: true
